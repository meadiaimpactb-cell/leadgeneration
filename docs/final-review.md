# المراجعة النهائية قبل الإصدار الأول

**تاريخ التنفيذ:** 2026-08-12 · على بيئة التطوير `leadgeneration.test` مع SSR
يعمل وبناء إنتاجي حديث.

**قاعدة هذا الملف:** كل ✅ أدناه ناتج فحص نُفِّذ فعلًا وسُجِّل أمره. وما لم أستطع
فحصه مكتوب **«لم يُفحص»** بصراحة مع السبب — لا استنتاج من قراءة الكود.

---

## ⚠️ ما لم أستطع فحصه — اقرأه أولًا

| البند | لماذا |
|---|---|
| **console المتصفح** (أخطاء JS، تحذيرات Vue، موارد 404) | لا أملك متصفحًا. فحصت الشبكة بـ`curl` وبناء الأصول ومسارات الملفات، **لكن أخطاء وقت التشغيل في المتصفح لم تُفحص إطلاقًا** |
| السلوك البصري الحي: انزلاق الـ toast، شريط التقدم، التوقف عند المرور، فتح قائمة الدول | كلها تعمل بعد تركيب JavaScript في متصفح حقيقي |
| العرض على الجوال والشاشات الصغيرة | لا أستطيع تغيير حجم نافذة |
| حالة فشل الإرسال مع قطع الاتصال | تتطلب قطع الشبكة من المتصفح |
| الخطوط والألوان كما تُرى | فحصت القيم في الكود لا ما يظهر على شاشة |

**هذه الخمسة تحتاج عشر دقائق منكم أمام المتصفح، وهي الفرق بين «مُختبَر» و«يبدو
صحيحًا».**

---

## الجزء 1 — الفافيكون ✅

| الفحص | النتيجة |
|---|---|
| `favicon.ico` كان **صفر بايت** | ✅ أُصلح — 2,349 بايت، ثلاثة أحجام (16/32/48) |
| توليد الأحجام القياسية | ✅ سبعة ملفات، كلها تستجيب 200 بنوع المحتوى الصحيح |
| `site.webmanifest` بالاسم ولون Navy | ✅ يستجيب 200 |
| الوسوم في القالب المشترك | ✅ `resources/views/app.blade.php` — قالب واحد يرثه الجميع |
| تظهر في صفحات الزائر (ar/en) | ✅ 4 وسوم `rel="icon"` على `/ar` و`/en` |
| تظهر في اللوحة وتسجيل الدخول | ✅ 4 وسوم على `/admin/login` |
| تظهر في صفحات الخطأ | ✅ بعد إنشاء صفحات الخطأ (الجزء 3) — كانت **صفرًا** قبلها |
| وضوح الشعار عند 16×16 | ✅ قرار موثّق: **الرمز لا اللقب** |

**قرار الشعار — بعد طلبكم:** كانت الأيقونة **نسيج السدو** بحجّة أن سطرين من الخط
لا ينجوان عند 16×16. طلبتم الشعار الحقيقي، فالشعار الحقيقي هو المرسوم الآن:
**اللقب المكدَّس** (`logo-stacked.svg`)، لأنه عند 516×470 شبه مربع فيملأ مربع
الأيقونة بدل أن يُحشر في شريط.

**وقاعدة الحجم:** فوق 32px يُرسم اللقب كاملًا بسطريه (الأيقونة على iOS وAndroid
والمفضّلة). **وعند 16 و32 يُرسم القسم العربي «أمد الحرف» وحده** — اللقب الكامل عند
ستة عشر بكسلًا يضع «Amad Craft» في أربعة بكسلات ارتفاعًا فيصير لطخة رمادية تحت
العربية تجعل الأيقونة تبدو معطوبة لا صغيرة؛ وإسقاط السطر اللاتيني يضاعف ارتفاع
الحرف تقريبًا فيعود الشعار معروفًا. وهذا ما يُظهره الفحص البصري للملفات المولَّدة.

**التوليد** بـ`scripts/make-favicons.mjs` عبر `sharp` (موجودة أصلًا كاعتمادية
Vite، وتُصيّر SVG). المولّد السابق بـGD حُذف: GD يرسم أشكالًا أولية ولا يُصيّر
مسارات Bézier، فلم يكن يستطيع رسم الشعار أصلًا.

---

## الجزء 2 — المراجعة الحيّة للصفحة الرئيسية

### 2.1 العرض ومصدر البيانات ✅

استُخرجت حمولة Inertia من `/ar` و`/en` وقُورنت.

| القسم | المصدر | ar | en | ملاحظة |
|---|---|---|---|---|
| `news_ticker` | `sections` + `section_translations` | ✅ heading+body+cta | ✅ | |
| `hero` | `sections` + وسائط القسم | ✅ heading+sub+cta+image | ✅ | |
| `intro_statement` | `section_translations.body` | ✅ body | ✅ | بلا عنوان بالتصميم |
| `solutions_grid` | `solutions` + ترجماتها | ✅ + 4 صفوف | ✅ | |
| `gallery` | `media_attachments` → `media` | ✅ 6 صور | ✅ | من مكتبة الوسائط |
| `sector_spotlight` | `sectors` + ترجماتها | ✅ + 4 صفوف | ✅ | |
| `stats` | `impact_metrics` | ✅ + 4 صفوف | ✅ | |
| `story_carousel` | `stories` + وسائط `person` | ✅ + 3 صفوف | ✅ | |
| `logos` | `partners` + وسائط `logo` | ✅ + 5 صفوف | ✅ | |
| `cta_band` | `sections` + `lead_fields` | ✅ | ✅ | |
| الهيدر/الفوتر | `navigations` + `settings` | ✅ | ✅ | |
| الخريطة | `settings.contact.map_*` | ✅ | ✅ | يرسمها الفوتر |

**صفر قيم ثابتة:** كل نص في الجدول أعلاه جاء من قاعدة البيانات — أُثبت بأن تعديله
من اللوحة غيّر الصفحة (2.2).

### 2.2 التحرير الحي — نُفِّذ على كل قسم ✅

لكل قسم: تسجيل دخول حقيقي إلى اللوحة → `PATCH` على القسم بنص علامة → جلب `/ar` →
التأكد من ظهور العلامة → إرجاع القيمة الأصلية → التأكد من اختفائها.

```
id   type                appeared  restored
147  news_ticker         YES       YES
148  hero                YES       YES
149  intro_statement     YES       YES
150  solutions_grid      YES       YES
151  gallery             YES       YES
152  sector_spotlight    YES       YES
153  stats               YES       YES
154  story_carousel      YES       YES
155  logos               YES       YES
156  cta_band            YES       YES
```

**وتأكيد سلامة الإرجاع** — قورنت الحمولة قبل الجولة وبعدها:

```
/ar: 11 sections, content differences after the edit round-trip: NONE
/en: 11 sections, content differences after the edit round-trip: NONE
```

### 2.3 الصور والترتيب والتفعيل ✅

| الفحص | النتيجة |
|---|---|
| تبديل صورة المعرض من المكتبة | ✅ الصورة 26 حلّت محل الأولى وظهرت على `/ar`، ثم أُرجعت |
| إعادة ترتيب قسمين | ✅ `gallery` و`solutions_grid` تبادلا الموضع على الصفحة، ثم أُرجع الترتيب |
| تعطيل قسم | ✅ اختفى `gallery` من الحمولة، **والصفحة سليمة والجيران بلا تغيير**، ثم أُعيد |

### 2.4 الروابط ✅ (بخلل واحد)

| الفحص | النتيجة |
|---|---|
| روابط داخلية على `/ar` تشير إلى `/en` | ✅ صفر |
| روابط داخلية على `/en` تشير إلى `/ar` | ✅ صفر |
| مرساة `#lead` موجودة في اللغتين | ✅ |
| مبدّل اللغة يشير إلى الوجهة فقط | ✅ `/ar` → en، `/en` → ar |
| **36 رابطًا فُحصت فعليًا** | ❌ **واحد مكسور** |

```
404  /documents/amad-craft-profile.pdf
```

**«حمّل ملف الشركة»** في الـ hero يشير إلى ملف غير موجود. **لم أصلحه عمدًا:**
الإصلاح إما اختلاق ملف تعريف شركة (محتوى، §22.1 يمنعه) أو حذف زر من تصميم
الزائر (قراركم). **الملف الحقيقي مطلوب منكم قبل الإطلاق.**

### 2.5 دورة النموذج ✅

| الفحص | النتيجة |
|---|---|
| إرسال **مع** رسالة | ✅ 302 وحُفظ |
| إرسال **بدون** رسالة | ✅ 302 وحُفظ |
| الرقم بصيغة E.164 | ✅ `+966 54 232 7104` → `+966542327104`، و`0512345678` → `+966512345678` |
| ظهوره في «العملاء المحتملون» | ✅ بالتاريخ ووسيلة التواصل والرسالة والمصدر والحالة و`crm_status=pending` |
| الرقم يُعرض مقروءًا | ✅ `+966 51 234 5678` في شاشة العميل، والمخزَّن E.164 كما هو |
| حروف في الهاتف | ✅ مرفوض 422 |
| رقم أقصر من قواعد دولته | ✅ مرفوض 422 |
| بريد بلا نطاق (`buyer@ministry`) | ✅ مرفوض 422 |
| وسيلة تواصل فارغة | ✅ مرفوض 422 |
| الرسائل بالعربية | ✅ «حقل رقم الهاتف مطلوب.» — لا مفاتيح خام |
| تحديد المعدل (5/دقيقة) | ✅ يعمل — الطلب السادس رُدّ، و`X-RateLimit-Remaining` يتناقص |
| **عمود الرسالة في اللوحة** | ❌ **أُصلح** — انظر أدناه |

**عمود الرسالة:** كان محدود العرض (`24rem`) وبلا اقتطاع، فرسالة من 497 حرفًا
(وأحدهم ألصق سيرته الذاتية كاملة) تصير صفًا بخمسة عشر سطرًا يزيح بقية العملاء عن
الشاشة. أُصلح: اقتطاع إلى سطرين مع `title` للنص الكامل، والنص كاملًا في صفحة
العميل التي يفتحها رابط وسيلة التواصل.

### 2.6 ما وجدته ولم أغيّره

| البند | الحالة |
|---|---|
| **قسم `hero` فارغ (id 219)** على الرئيسية، مفعّل، بلا نص ولا إعدادات | مرصود — بيانات لا كود. لا يعرض شيئًا (الصفحة ترسم أول hero فقط)، لكنه سيُرحَّل إن نُسخت هذه القاعدة إلى الإنتاج. **احذفوه من اللوحة قبل النشر** |
| `/documents/amad-craft-profile.pdf` مكسور | يحتاج ملفكم |

---

## الجزء 3 — نظافة الكود وجاهزية النشر

### 3.1 الفحوص الآلية

| الفحص | النتيجة |
|---|---|
| `console.log` / `debugger` في Vue أو JS | ✅ صفر |
| `dd()` / `dump()` / `var_dump()` في PHP | ✅ صفر |
| مفاتيح أو كلمات مرور في الكود | ✅ صفر |
| `env()` خارج `config/` | ✅ صفر |
| مكوّنات Vue غير مستعملة | ⚠️ واحد — `sections/MapBlock.vue` |
| `npm run build` | ✅ ينجح، الحزمة الأولية ≈69KB مضغوطة (السقف 180KB) |
| Pint | ✅ نظيف |
| **الحزمة الكاملة للاختبارات** | انظر 3.4 |

**`MapBlock.vue`:** غير مستورد في أي مكان — الفوتر يرسم الخريطة من `settings`
بنفسه. **لم أحذفه**، لأن النوع `map` ما زال في `Section::TYPES` فيستطيع المحرّر
إضافته من اللوحة ولن يظهر شيء. الحذف يزيل الحل الوحيد الممكن، والتسجيل في
`SectionRenderer` يغيّر ما يراه الزائر — **والقراران لكم**.

### 3.2 ما حُذف

- `App\Support\Brand::forget()` — كود ميت **كان سيرمي خطأً قاتلًا لو استُدعي**:
  يستعمل `Cache` بلا استيراد و`self::CACHE_KEY` غير المعرَّف. بقايا من إزالة
  تخزين مؤقت سابق. لا يستدعيه شيء.
- `ui/ResultDialog.vue` — حلّ محله الـ toast (حُذف في التزام سابق).

### 3.3 ما أُضيف للجاهزية

| البند | الحالة |
|---|---|
| صفحات 404 / 500 / 503 بهوية الموقع | ✅ كانت صفحات Laravel الإنجليزية على `<html lang="en">` |
| لغة صفحة الخطأ تتبع الرابط | ✅ `/en/nope` → إنجليزية، `/ar/nope` → عربية، والجذر → الافتراضية |
| `.env.example` | ✅ أُضيفت متغيّرات الوسائط وكاش الصفحات المطلوبة إنتاجيًا |
| `docs/deployment.md` | ✅ بما فيه تحويل النسخ الاحتياطي إلى cron |

**صفحات الخطأ Blade لا Inertia عمدًا:** خطأ 500 يعني أن شيئًا في التطبيق سقط
بالفعل، وصفحة تحتاج إقلاع الإطار وحلّ الـ props ورسم Vue لتقول «حدث خطأ» هي صفحة
لن تظهر في اللحظة التي وُجدت لها. هذه markup ثابتة بأنماط مضمّنة وبلا بناء.

**وعلّة كشفها الفحص:** خطأ 404 لا يطابق مجموعة المسارات ذات بادئة اللغة، فلا
يعمل `SetLocale` أبدًا — فكانت صفحة الخطأ العربية تُقدَّم لمن أخطأ في رابط `/en`.
اللغة تُقرأ الآن من أول مقطع في الرابط.

### 3.4 الاختبارات

```
Tests:    1 failed, 391 passed  (1447 assertions)
Duration: 896.70s
```

**391 اختبارًا خضراء، وواحد أحمر معروف** — وهو الوحيد في الحزمة كلها.

**الاختبار الأحمر الوحيد المعروف** — `TrainingPageServesTwoAudiencesTest`:
يؤكد أن النموذج يحمل **ثلاثة** حقول ويحمل الآن **أربعة** (اسم الشركة، الهاتف،
البريد، والرسالة الاختيارية التي أعادها «القرار A»). سابق لهذه الجلسة، وقراره
قراركم: تحديث الرقم إلى 4 بتعليق يذكر القرار، أو إطفاء حقل الرسالة. **لم أغيّره
لأن تعديل رقم في اختبار حتى يصير أخضر هو كيف يفقد الاختبار قيمته.**

---

## ما يجب أن تنتبهوا له قبل النشر

1. **ملف تعريف الشركة (PDF) مفقود** — زر «حمّل ملف الشركة» في الـ hero يشير إلى
   `/documents/amad-craft-profile.pdf` وهو 404. ارفعوا الملف أو أزيلوا الزر.
2. **القسم الفارغ id 219** على الرئيسية — احذفوه من اللوحة قبل نسخ قاعدة
   البيانات إلى الإنتاج.
3. **الاختبار الأحمر** أعلاه — قراركم.
4. **`APP_DEBUG=false`** في الإنتاج. مع `true` تُعرض آثار الاستثناءات للزوار.
5. **العمليتان الدائمتان** — بلا `queue:work` لا يصل عميل إلى CRM ولا تُولَّد
   أحجام الصور؛ وبلا `inertia:start-ssr` يُقدَّم HTML فارغ لمحركات البحث.
   **وأعيدوا تشغيل SSR بعد كل بناء** — وقعت هذه مرتين أثناء التطوير.
6. **اختبروا استعادة واحدة على الخادم الحقيقي** بعد إعداد cron. نسخة لم تُستعد
   ليست نسخة.
7. **الخمسة التي لم تُفحص** في أعلى هذا الملف — عشر دقائق أمام المتصفح.
8. **مفاتيح التتبّع** (GA4، GTM، Pixel، Clarity) تُدخل من شاشة الإعدادات بعد
   النشر، لا من الكود (§14.1).
9. **الوسائط في النسخ الاحتياطي** — سكربت لينكس في `docs/deployment.md` يشملها؛
   سكربت ويندوز لا يشملها (فجوة مسجّلة في `docs/backup.md`).
